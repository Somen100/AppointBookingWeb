import { DateGroupPipe } from './date-group.pipe';

describe('DateGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new DateGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
